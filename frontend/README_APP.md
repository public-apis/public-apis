# 公共 API 浏览器

一个基于 React + TypeScript 构建的公共 API 浏览器应用，帮助开发者发现和使用免费的公共 API。

## ✨ 功能特性

### 🔍 搜索与过滤
- **即时搜索**: 按 API 名称或描述进行实时搜索
- **分类过滤**: 按 API 分类进行筛选
- **HTTPS 过滤**: 仅显示支持 HTTPS 的 API
- **认证过滤**: 按认证方式筛选（无需认证、API Key、OAuth 等）

### 📋 分页展示
- 每页展示 20 条 API 条目
- 智能分页导航，支持快速跳转

### ⭐ 收藏功能
- 使用 localStorage 本地存储收藏的 API
- 一键收藏/取消收藏
- 收藏状态持久化保存

### 📝 详情弹窗
- 点击查看 API 完整信息
- 展示 API 地址、协议、认证方式、CORS 支持等详细信息
- 支持直接跳转到官方网站

### 📱 响应式设计
- 完美适配桌面端和移动端
- 现代化的 UI 设计

## 🚀 启动步骤

### 前置要求
- Node.js >= 16.0.0
- npm >= 8.0.0

### 安装依赖

```bash
cd frontend
npm install
```

### 启动开发服务器

```bash
npm run dev
```

应用将在 `http://localhost:5173` 启动。

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 📂 项目结构

```
frontend/
├── public/
│   ├── apis.json          # API 数据文件
│   └── index.html
├── src/
│   ├── components/        # React 组件
│   │   ├── APICard.tsx    # API 卡片组件
│   │   ├── DetailModal.tsx# 详情弹窗组件
│   │   ├── Filter.tsx     # 过滤器组件
│   │   └── Pagination.tsx # 分页组件
│   ├── hooks/
│   │   └── useAPI.ts      # 自定义 Hook，管理 API 数据和状态
│   ├── types/
│   │   └── api.ts         # TypeScript 类型定义
│   ├── App.tsx            # 主应用组件
│   ├── App.css            # 全局样式
│   └── main.tsx           # 应用入口
└── package.json
```

## 🎨 技术栈

- **React 18**: UI 框架
- **TypeScript**: 类型安全
- **Vite**: 构建工具
- **CSS3**: 样式设计
- **localStorage**: 本地存储

## 📊 数据来源

API 数据来自 [public-apis](https://github.com/public-apis/public-apis) 项目，包含超过 80 条精选 API。

如果无法加载线上数据，应用会自动使用模拟数据运行。

## 📄 License

MIT License
